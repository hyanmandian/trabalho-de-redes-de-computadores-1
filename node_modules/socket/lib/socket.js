'use strict';

let server = require('./server');
let client = require('./client');

class Socket {
    
    createServer(port, host) {
        
        return server.createServer(port, host);
        
    }
    
    createConnection(port, host) {
    
        return client.createConnection(port, host);
        
    }
    
}

module.exports = new Socket();