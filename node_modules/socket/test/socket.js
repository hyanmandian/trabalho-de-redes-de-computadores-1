'use strict';

var assert = require('assert');
var socket = require("socket");

describe('Socket', () => {
    
    describe('.createServer', () => {
        
        it('should return todo', (done) => {
        
        });
        
    });
    
});